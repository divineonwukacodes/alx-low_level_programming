#include <stdio.h>

/** 
 * main-Prints a new name followed by a newline
 * 0agrc: arguments count
 * 0agrv: arguments vector
 * Return: 0
 */

int main(int agrc, char *agrv[])
{
	printf("%d/n", agrc) :
	return (0);
}
