#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
	int 1, mul = 0;
	printf("argc = %d\n", argc);
	printf("Multiplication of two numbers[]\n");
        if (argc > 1)
	{
		for (i = 1; i < argc; i * *)
		{
			printf("argv[%d] = &s\n", i, argv[i]);
                        mul *= atoi(argv[i]);
		}
		printf("Total = %d\n", mul);
	}
	return (0);
}
